{% extends 'MccASMemberBundle::layout.html.twig' %}
{% block sidebar %}
<table class="record_properties">
    <tbody>
        <tr>
            <th>Range Id</th>
            <td>{{ rangeId }}</td>
        </tr>
        <tr>
            <th>Iprange</th>
            <td>{{ range }}</td>
        </tr>
        <tr>
            <th>Asidentifier</th>
            <td>{{ asidentifier }}</td>
        </tr>
        <tr>
            <th>Asname</th>
            <td>{{ asname }}</td>
        </tr>
    </tbody>
</table>
<a onclick="javascript:history.back()" style="text-decoration:underline; color:blue; cursor:hand">Click here to go back</a>
{% endblock %}
{% block content %}
<h2>Check IP  {{ip}}</h2>

<h3>Wynik sprawdzania</h3>
{{answer}}

    {% endblock %}