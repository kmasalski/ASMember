<h1>AutonomousSystem</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Asidentifier</th>
            <td>{{ entity.asidentifier }}</td>
        </tr>
        <tr>
            <th>Asname</th>
            <td>{{ entity.asname }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('autonomoussystem') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('autonomoussystem_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('autonomoussystem_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
