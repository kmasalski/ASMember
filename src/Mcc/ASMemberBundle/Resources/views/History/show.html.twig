<h1>History</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Whenchecked</th>
            <td>{{ entity.whenchecked|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Iswebserver</th>
            <td>{{ entity.iswebserver }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('history') }}">
            Back to the list
        </a>
    </li>
</ul>
